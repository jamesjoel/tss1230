var user = {
    name : "Rohit",
    age : 25,
    city : "indore",
    gender : "male",
    contact : 48784,
    salary : 5000
}

// var { name, age, ...demo } = user;

// var info = {...user, salary : 1000};

// console.log(info)

var demo = { a : 10, b : 20, c : 30, a : 50 };

console.log(demo);

